// Description:
//    Programme  avec une simple declaration.
//
// Resultats:

{
	int x = 2;
	int y = x + 4;
	boolean z = true;
	//float f = 2,5;
	println(x, y + 3);
	if (x < 2) {print(x);} else {print(y);};
	while (x < 4) {
		println(x);
		x = x + 1;
	};
}
